#include <stdio.h>
#define TAM 9
int main()
{
	int V[TAM], i;
	printf("Insira 10 valores: ");
	for (i = 0; i < 10; i++)
		scanf("%d", &V[i]);
	for (i=0; i<9; i++)
		printf("%d\t", V[i]);
	printf("%d\n", V[9]);
	for (i=0; i<10; i++)
		printf("%d\n", V[i]);
	return 0;
}
